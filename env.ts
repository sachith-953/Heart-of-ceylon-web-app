// this use .env File. if you want add new AlignVerticalDistributeStart, then add them in both files 

const env = {
    URL: process.env.URL || '',
    PRODUCTS_PER_PRODUCT_SEARCH_RESULT: process.env.PRODUCTS_PER_PRODUCT_SEARCH_RESULT || '',
  };
  
  export default env;


